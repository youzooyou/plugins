{"category": "logic_off_by_one", "difficulty": "subtle", "should_flag": true, "must_mention_file": "movingAverage.js", "keywords_any": ["off-by-one", "off by one", "never removed", "never subtracted", "stale", "sliding window", "boundary", "windowsize"], "severity_floor": "medium", "defect_assertion": "The eviction check uses i > windowSize instead of i >= windowSize, so each iteration evicts the wrong, one-too-recent index from the running sum instead of the value that just left the window; the very first value in the series is therefore never subtracted out and stays baked into every subsequent average indefinitely, permanently skewing the rolling average rather than producing just one bad early value."}
