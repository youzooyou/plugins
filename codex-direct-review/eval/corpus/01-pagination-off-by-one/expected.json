{"category": "logic_off_by_one", "difficulty": "easy", "should_flag": true, "must_mention_file": "paginate.py", "keywords_any": ["off-by-one", "off by one", "offset", "page - 1", "(page-1)", "pagination"], "severity_floor": "medium", "defect_assertion": "The function treats page as 0-indexed when computing start = page * page_size, but page is documented and validated as 1-indexed, so every call skips an entire page worth of items -- page 1 returns what should be page 2's items, and the true first page is never returned at all."}
