{"category": "performance_regression", "difficulty": "subtle", "should_flag": true, "must_mention_file": "orders.js", "keywords_any": ["n+1", "cache", "instantiated inside", "new map()", "never persists", "still queries", "per order", "scope"], "severity_floor": "medium", "defect_assertion": "getCustomerName instantiates cache = new Map() locally inside the function body, so a fresh, empty cache is created and discarded on every single call instead of persisting across calls; attachCustomerNames calls it once per order in a loop, so the cache check always misses and the function still issues one database query per order -- a hidden N+1 despite looking like it should short-circuit repeat lookups."}
