{"category": "api_breaking_change", "difficulty": "subtle", "should_flag": true, "must_mention_file": "checkout.py", "keywords_any": ["reorder", "swapped", "argument order", "positional", "wrong order", "quantity and tax_rate", "silently"], "severity_floor": "high", "defect_assertion": "checkout() calls calculate_total(price, quantity, tax_rate) positionally, but the real signature in pricing.py is calculate_total(price, tax_rate, quantity) -- the second and third arguments are swapped, so the quantity value silently gets used as tax_rate and vice versa; since both are plain floats passed positionally nothing raises an error, it just produces a silently wrong total."}
