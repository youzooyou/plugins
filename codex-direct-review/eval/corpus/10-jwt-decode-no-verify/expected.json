{"category": "security_injection", "difficulty": "subtle", "should_flag": true, "must_mention_file": "auth.js", "keywords_any": ["includes", "substring", "privilege escalation", "admin_view_only", "role name", "exact match", "startswith", "contains"], "severity_floor": "high", "defect_assertion": "isAdmin grants admin status if any of the user's role strings merely contains the substring admin (via includes()) rather than being exactly equal to it, so a lesser role like admin_view_only would incorrectly satisfy the check and let a non-admin user pass the isAdmin gate protecting the destructive delete_all_records action -- a privilege escalation from substring rather than exact role matching."}
