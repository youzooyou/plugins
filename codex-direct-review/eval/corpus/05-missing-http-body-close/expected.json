{"category": "resource_leak", "difficulty": "easy", "should_flag": true, "must_mention_file": "fetch.go", "keywords_any": ["resp.body.close", "defer", "leak", "connection leak", "file descriptor", "not closed"], "severity_floor": "medium", "defect_assertion": "FetchBody never calls resp.Body.Close() (no defer immediately after the successful http.Get), so the underlying connection or file descriptor is leaked on every call, including the success path after io.ReadAll returns cleanly."}
