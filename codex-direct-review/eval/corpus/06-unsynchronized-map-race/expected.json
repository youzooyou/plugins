{"category": "concurrency_race", "difficulty": "easy", "should_flag": true, "must_mention_file": "cache.go", "keywords_any": ["race", "mutex", "concurrent map", "data race", "lock", "sync.mutex"], "severity_floor": "high", "defect_assertion": "IncrementAll spawns one goroutine per key that reads and writes the shared c.data map (c.data[key]++) with no mutex or other synchronization, so concurrent goroutines can race on the same underlying map -- a data race that can corrupt state or crash the program with Go's concurrent map write fatal error."}
