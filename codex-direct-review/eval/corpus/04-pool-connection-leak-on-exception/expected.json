{"category": "resource_leak", "difficulty": "subtle", "should_flag": true, "must_mention_file": "db_pool.py", "keywords_any": ["retry", "leak", "release", "never released", "except", "continue", "pool exhaust", "each retry"], "severity_floor": "medium", "defect_assertion": "When execute() raises TransientError, the except block retries via continue without ever calling pool.release(conn) for the connection that just failed; each retry acquires another connection from the pool while the failed one is never returned, so repeated transient failures leak connections and can exhaust the pool."}
